// import type { Champion, Item } from "~/types";
// import items from "../data/item.json";

// export function isItemDroppable(champion: Champion) {
//   if ((champion.item?.length || 0) < 3) return true;
//   if (champion.item) {
//     champion.item.filter((item) => {
//       const findItem = items.find((i) => (i.name = item.name));
//       return !!findItem?.composite_items;
//     });
//   }
// }
